@claude

Convert the Refiloe dashboard into a Progressive Web App (PWA) that can be installed on phones.

Requirements:

1. Create static/manifest.json with:
   - App name: "Refiloe Calendar"
   - Short name: "Refiloe"
   - Description
   - Icons (generate from Refiloe logo - 192x192, 512x512)
   - Start URL: /dashboard
   - Display: standalone
   - Theme color: Refiloe's beige (#...)
   - Background color: white
   - Orientation: portrait

2. Create static/service-worker.js for:
   - Cache calendar data for offline viewing
   - Cache static assets (CSS, JS, images)
   - Network-first strategy for API calls
   - Cache-first strategy for static assets
   - Show cached data when offline

3. Update templates/dashboard.html to:
   - Link to manifest.json
   - Register service worker
   - Add Apple-specific meta tags for iOS
   - Add install prompt for Android/Desktop
   - Show offline indicator when no connection

4. Add install promotion:
   - Detect if not installed
   - Show subtle install banner after 3 uses
   - "Add to Home Screen" button in settings

5. Implement offline functionality:
   - View cached calendar data offline
   - Queue actions for when online
   - Sync when connection restored

Test that the PWA can be installed and works offline for viewing calendar data.

@apply
