@claude @apply @sonnet Fix the Unicode escape syntax error in services/refiloe.py

In the _check_payment_status method around line 463, the Unicode escape for the money bag emoji is causing a syntax error.

### EDIT: services/refiloe.py

**Change 1:** Fix emoji Unicode escape in payment status message
Location: Lines 461-465

# REMOVE (lines 461-465):
                if outstanding.data:
                    total = sum(p['amount'] for p in outstanding.data)
                    message = f"""\U0001F4B0 *Payment Status*
Outstanding: R{total:.2f}
Contact your trainer for payment details."""

# ADD:
                if outstanding.data:
                    total = sum(p['amount'] for p in outstanding.data)
                    # Using money bag emoji
                    message = f"""💰 *Payment Status*

Outstanding: R{total:.2f}

Contact your trainer for payment details."""
